import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ListUserSortComponent } from './list-user-sort.component';

describe('ListUserSortComponent', () => {
  let component: ListUserSortComponent;
  let fixture: ComponentFixture<ListUserSortComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListUserSortComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ListUserSortComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
